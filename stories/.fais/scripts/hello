#!/bin/bash
# Example script - this file was generated by fais init
# You can delete this and add your own scripts here.
#
# Scripts can be run with: fais run <script_name> [args...]
# Use -s flag to select a story: fais run -s <script_name> [args...]
# The selected story path is available as $FAIS_STORY_DIR

echo "Hello from fais scripts!"
echo ""
echo "Arguments received: $@"
echo ""
if [ -n "$FAIS_STORY_DIR" ]; then
    echo "Story directory: $FAIS_STORY_DIR"
else
    echo "No story selected (use -s flag to select one)"
fi
